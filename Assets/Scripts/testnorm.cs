using UnityEngine;

public class testnorm : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // aaaaaaaaaaaaaaaaaaaaaaaaaaaaa test
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
