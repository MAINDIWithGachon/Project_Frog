using UnityEngine;

public class BoundingBoxOverride : MonoBehaviour
{
    [Tooltip("If > 0, ParallaxBackground will use this value as the object's world width.")]
    public float customWidth = 0f;
}
