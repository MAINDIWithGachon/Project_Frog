using UnityEngine;

public class script_sb : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("test 123");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
